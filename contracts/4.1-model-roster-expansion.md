# Task 4.1 -- Expand Model Roster and Provider Types

## Objective

Extend the model type system to support Google/Gemini models and a dedicated "research" tier. Add Gemini 3.1 Pro and Flash Lite pricing to the cost tracking module.

## Context

Phase 4 adds a Researcher agent that uses Gemini as its primary model. The current type system only supports `"anthropic"` and `"openai"` providers, and `"heavy"` / `"standard"` / `"light"` tiers. This task adds the foundation types and pricing that all subsequent Phase 4 tasks depend on.

No dependencies. Foundation task for Phase 4.

## Requirements

- [ ] Add `"research"` to `ModelTier` union type
- [ ] Add `"google"` to `Provider` union type
- [ ] Add Gemini 3.1 Pro pricing to `PRICING` table
- [ ] Add Gemini 3.1 Flash Lite pricing to `PRICING` table
- [ ] Add `research` entry to `DEFAULT_ROSTER`: primary Gemini 3.1 Pro (google), fallback Sonnet 4.6 (anthropic)
- [ ] Existing tests updated for new tier/provider (no regressions)
- [ ] New tests for Gemini cost calculations
- [ ] Verify model IDs match Google's API (`gemini-3.1-pro`, `gemini-3.1-flash-lite` -- confirm exact strings)

## Interface Contract

**Inputs:** None (type definitions).

**Outputs:** Expanded `ModelTier`, `Provider`, `ModelRoster`, and `PRICING` exports.

**Side Effects:** None.

## Technical Approach

The `ModelTier` union at `src/agents/types.ts:7` gains `"research"`. The `Provider` union at line 9 gains `"google"`. The `DEFAULT_ROSTER` gains a research entry. The `PRICING` map in `src/costs/pricing.ts` gains two Gemini models.

Pricing (verify against Google's current pricing page):
- `gemini-3.1-pro`: estimated $1.25 / $10.00 per 1M tokens (input/output)
- `gemini-3.1-flash-lite`: estimated $0.075 / $0.30 per 1M tokens (input/output)

These are estimates -- look up the actual pricing during implementation.

## Files to Create/Modify

- `src/agents/types.ts` -- expand `ModelTier`, `Provider`, `DEFAULT_ROSTER`
- `src/costs/pricing.ts` -- add Gemini model pricing
- `tests/costs.test.ts` -- add Gemini pricing tests

## Acceptance Criteria

- [ ] `bun run typecheck` passes
- [ ] `bun test` passes -- all new and existing tests pass
- [ ] `ModelTier` includes `"research"`
- [ ] `Provider` includes `"google"`
- [ ] `DEFAULT_ROSTER.research` maps to Gemini primary, Sonnet fallback
- [ ] `PRICING` includes both Gemini models
- [ ] `logUsage` and `getSessionCost` work correctly with Gemini models

## What NOT to Do

- Do not implement the Gemini adapter here -- that's Task 4.2
- Do not add BrowserUse or research types -- those are separate tasks
- Do not change the existing heavy/standard/light tier configurations

## References

- Google Gemini API pricing page
- `src/agents/types.ts` -- current ModelTier/Provider/ModelRoster
- `src/costs/pricing.ts` -- current PRICING table
